// Garante que o código só rode quando a página estiver pronta
$(document).ready(() => {
  // Seleciona o container e aplica a animação de fadeIn
  $('.curriculo-container').fadeIn(1000); // 1000ms = 1 segundo
});
